const env = {
    host: process.env.NEXT_PUBLIC_URL || 'https://the-cafe-revirew.herokuapp.com',
    backand: process.env.NEXT_PUBLIC_API_URL || 'https://the-cafe-revirew-api.herokuapp.com',
};

export default env;
